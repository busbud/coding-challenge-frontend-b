export enum colors {
  main = '#DF0B9E',
  default = '#111111',
  danger = '#ff0000',
}

export enum screenSize {
  small = '768px',
  medium = '945px',
}

export enum fontSize {
  md = 16,
  lg = 24,
  xlg = 40,
}

export const theme = {
  colors,
  fontSize,
  screenSize,
}
