.__SCOPE {
  align-items: center;
  display: flex;
  flex-direction: column;
  justify-content: center;
  pointer-events: none;
  width: 100%;
  height: 100%;

  > svg {
    display: block;
    margin-bottom: 5px;
  }

  > p {
    color: #000;
    font-size: 12px;
  }
}
