@import 'uiAssets/vars.pcss';

.__SCOPE {
  text-align: left;

  > h3 {
    font-size: 20px;
    font-weight: bold;
  }

  > p {
    line-height: 1.3;
    margin: 8px 0;
  }

  > hr {
    margin: 15px 0;
    border: 1px dashed #666;
  }
}
