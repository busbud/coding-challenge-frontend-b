export enum VehicleType {
    FREENOW = 'FREENOW',
    SHARENOW = 'SHARENOW'
}

export enum RateType {
    GOOD = 'GOOD',
    UNACCEPTABLE = 'UNACCEPTABLE'
}

export enum State {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
    UNKNOWN = 'Unknown',
}