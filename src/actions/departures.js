export const INITIALIZE_DEPARTURE_SEARCH = 'INITIALIZE_DEPARTURE_SEARCH'

export const getDepartures = (originHash,destinationHash,outboundDate) => {
    
        return (dispatch) => {
           
        } 
    }