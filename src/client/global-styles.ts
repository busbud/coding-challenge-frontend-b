import { createGlobalStyle } from './dependencies';

export const GlobalStyle = createGlobalStyle`
  html, body {
    margin: 0;
    cursor: default;
  }
  body{
    font-family: Merriweather;
  }
`;
