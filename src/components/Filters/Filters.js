import React from 'react';
import './Filters.scss';

class FiltersList extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <div>FiltersList</div>
    );
  }
}

export default FiltersList;