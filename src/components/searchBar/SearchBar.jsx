import React from "react";
import styled from "styled-components";

const Card = styled.div`
  border-radius: 4px;
  box-shadow: 2px 5px 4px #555;
  background: white;
  padding: 5px;
  margin: 10px;
`;

export const SearchBar = () => {
  return <Card>search bar</Card>;
};
