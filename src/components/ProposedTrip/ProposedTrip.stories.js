// @flow
import React from 'react';
import { storiesOf } from '@storybook/react';
import { ProposedTrip } from './ProposedTrip';

storiesOf('ProposedTrip', module).add('default', () => <ProposedTrip />);
