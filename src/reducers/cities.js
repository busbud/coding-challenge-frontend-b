const cityState = {
    origin:null,
    destination:null,
    locations:[]
}

export default (state = cityState, action) =>{
    switch(action.type){
        default:
            return state
    }
}