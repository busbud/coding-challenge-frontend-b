import React from "react";
import styled from "styled-components";

const Loader: React.FC = () => <Container> Loader </Container>;

const Container = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
`;

export default Loader;
