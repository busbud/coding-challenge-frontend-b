{
  "name": "busbud-challenge-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "heroku-prebuild": "cd busbud-challenge && npm install && npm run build",
    "start": "cd busbud-challenge && npm start"
  }
}