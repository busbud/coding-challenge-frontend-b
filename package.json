{
  "name": "busbud-challenge-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd busbud-challenge && npm build",
    "start": "cd busbud-challenge && npm install && npm run build"
  }
}